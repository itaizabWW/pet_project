<%= form_with(model: restaurant, local: true) do |form| %>
  <% if restaurant.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(restaurant.errors.count, "error") %> prohibited this restaurant from being saved:</h2>

      <ul>
      <% restaurant.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :restaurant_name %>
  </div>

  <div class="field">
    <%= form.label :cuisine_id %>
    <%= form.collection_select :cuisine_id,  @cuisines, :id ,:name %>
  </div>

  <!--<div class="field">-->
    <!--<%= form.label :rating %>-->
    <!--<%= form.select(:rating, [['0',0],['1',1] ,['2',2], ['3',3]])%>-->
  <!--</div>-->

  <div class="field">
    <%= form.label :tenbis %>
    <%= form.check_box :tenbis, id: :restaurant_10bis %>
  </div>

  <div class="field">
    <%= form.label :address %>
    <%= form.text_field :address, id: :restaurant_address %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.text_field :price, id: :restaurant_price %>
  </div>

  <div class="field">
    <%= form.label :maxDevTime %>
    <%= form.select(:maxDevTime, [['0',0],['15',15] ,['30',30], ['60',60]])%>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
